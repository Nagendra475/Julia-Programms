x1 = parse(Int, readline())
y1 = parse(Int, readline())
x2 = parse(Int, readline())
y2 = parse(Int, readline())
midX = (x1 + x2) / 2.0
midY = (y1 + y2) / 2.0
println("Arun's house is located at($(round(midX, digits=1)),$(round(midY, digits=1)))")
